development:
  secret_key_base: <%= SecureRandom.hex(64) %>
  session_store_key: _feedback_development_session_key
  recaptcha:
    site_key: <%= SecureRandom.base64(24) %>
    secret_key: <%= SecureRandom.base64(24) %>
test:
  secret_key_base: <%= SecureRandom.hex(64) %>
  session_store_key: _feedback_test_session_key
  recaptcha:
    site_key: <%= SecureRandom.base64(24) %>
    secret_key: <%= SecureRandom.base64(24) %>
